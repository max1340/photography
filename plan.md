# План разработки проекта (plan.md)

## Завершено: Превращение модалки поста блога в полнофункциональный редактор (Rich Editor)

- [x] 1. **TDD Тесты**: Разработка набора тестов в `test_admin_suite.cjs` для проверки всех 9 требований редактора (30 проверок).
- [x] 2. **Разметка `admin.html`**:
  - [x] Двухколоночный контейнер `.modal-body-layout` (`.post-col-edit` и `.post-col-preview`).
  - [x] Мобильный тумблер колонок `#postMobileTabs` для экранов < 1000px.
  - [x] Markdown-тулбар `#mdToolbar` над полем `#editField3`.
  - [x] Счетчик фото `#photoCounter`, подсказка лида `#leadHint`, счетчик символов и времени чтения `#contentStats`.
  - [x] Контейнер живого превью `#postPreview`.
- [x] 3. **Стили `admin.css`**:
  - [x] Стили для `.modal-box--wide` (max-width: 860px).
  - [x] Двухколоночная сетка (на ≥1000px flex/grid, справа липкий превью `position: sticky; top: 0`).
  - [x] Стили тумблера «Текст / Превью» для экранов < 1000px.
  - [x] Стили тулбара Markdown `.md-toolbar` и кнопок `.btn-tool`.
  - [x] Сетка тайлов фото $120\times 120$px с gap 10px, бейдж обложки, кнопки действий (←, →, обложка, ×), спиннер загрузки.
  - [x] Стили карточки живого превью `#postPreview` в стилистике сайта (`.bdate`, заголовки, параграфы, фото).
  - [x] Стили ошибок валидации (`.inp.err`, `.ph-upload.err`).
- [x] 4. **Логика `admin.js`**:
  - [x] Активация `.modal-box--wide` и переключение колонок при `currentMode === 'posts'`.
  - [x] Функция `updatePostPreview()`: синхронный рендер фото, даты, заголовка, лида и контента (с `mdPreview()`).
  - [x] Обработчики кнопок Markdown-тулбара (`selectionStart`/`selectionEnd`).
  - [x] Поддержка Drag & Drop для `.ph-upload` (dragover, drop) с показом спиннера.
  - [x] Управление фото: удаление, перемещение `←`/`→`, назначение обложки (индекс 0).
  - [x] Автосохранение черновиков (`draft_post_...`) в `localStorage` с дебаунсом 500мс и восстановлением.
  - [x] Флаг `isDirty` и защита от случайного закрытия модалки через `toastAction`.
  - [x] Валидация при сохранении поста (заголовок + фото) с красными рамками `.err`.
  - [x] Расчет символов и времени чтения, показ подсказки для пустого лида.
- [x] 5. **Верификация и тестирование**:
  - [x] Запуск всех тестовых наборов `test_admin_suite.cjs`, `test_gallery.cjs`, `test_roles.cjs`, `test_styles.cjs` — 100% PASS (0 ошибок).
  - [x] Проверка сохранения совместимости с режимами `hero` и `works`.
