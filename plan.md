# План работ (plan.md) — Внедрение страницы галереи

- [x] 1. Создание TDD тестового скрипта (`test_gallery.cjs`)
- [x] 2. Создание `gallery.html` на основе референса (однофайловая структура, подключение Firebase `works_${currentLang}`, фильтры `?cat=`, лайтбокс, `loadMore` по 24 шт., адаптивность)
- [x] 3. Обновление `main.js` (переход на `gallery.html` по кнопке «СМОТРЕТЬ ВСЕ», поддержка `.seeall`)
- [x] 4. Очистка `index.html` от неиспользуемой модалки `#fullGalleryModal`
- [x] 5. Запуск TDD тестов (100% PASS)
- [x] 6. Браузерная валидация (проверка реальных данных, фильтрации, `?cat=`, лайтбокса, мобильной адаптивности)
- [x] 7. Документирование результатов
